<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Happy birthday my favourite one</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <style>
    html,body{height:100%;margin:0;padding:0}
    body {
      margin: 0;
      padding: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      background: url('background.jpg') no-repeat center center/cover;
      font-family: 'Comic Sans MS', 'Trebuchet MS', sans-serif;
      overflow: hidden;
      color: #fff;
    }
    .card{
      width: min(980px,94vw);
      padding: 30px;
      text-align: center;
      position: relative;
      z-index: 2;
    }
    h1{
      margin:0 0 12px 0;
      font-size: clamp(28px,4.4vw,56px);
      letter-spacing: 1px;
      line-height: 1.05;
      text-shadow: 0 0 12px rgba(255,0,150,0.9), 0 0 28px rgba(0,200,255,0.8);
      animation: glow 2s infinite alternate;
    }
    @keyframes glow{
      from { text-shadow: 0 0 12px rgba(255,0,150,0.9), 0 0 28px rgba(0,200,255,0.8); }
      to   { text-shadow: 0 0 24px rgba(0,200,255,0.9), 0 0 48px rgba(255,0,150,0.8); }
    }
    p.message{
      max-width: 900px;
      margin: 8px auto 16px auto;
      font-size: clamp(14px,2.1vw,20px);
      line-height: 1.6;
      text-shadow: 0 2px 6px rgba(0,0,0,0.6);
      color: #fff;
      background: linear-gradient(90deg, rgba(0,0,0,0.12), rgba(0,0,0,0.08));
      padding: 14px;
      border-radius: 10px;
    }

    /* Decorative images */
    .decor{position:absolute;pointer-events:none;z-index:1}
    .left-balloons{left:10px;bottom:10px;width:220px;max-width:28vw}
    .right-couple{right:10px;bottom:10px;width:250px;max-width:30vw}
    .top-stars{left:12vw;top:6vh;width:120px}

    /* Confetti (simple CSS falling pieces) */
    .confetti-piece{
      position:fixed;
      width:12px;height:8px;border-radius:4px;
      opacity:0.95;
      animation: confetti-fall linear infinite;
      z-index:1;pointer-events:none;
    }
    @keyframes confetti-fall{
      0%{transform:translateY(-10vh) rotate(0deg);opacity:1}
      100%{transform:translateY(110vh) rotate(720deg);opacity:0}
    }

    /* make audio controls visible on small devices */
    audio{position:fixed;left:12px;top:12px;z-index:5;opacity:0.95}

    @media (max-width:520px){
      .left-balloons, .right-couple{display:none}
      .card{padding:20px}
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>🎉 HAPPY BIRTHDAY CUTEEIIII 🎉</h1>
    <p class="message">
      Happy birthday to the person I love most ❤️<br>
      You are the reason of happiness in my life.<br>
      And I can't thank you enough for it.<br><br>
      On your birthday..<br>
      I wish that you receive as much happiness as you have given me and stay blessed always.<br>
      Always stay happy.<br><br>
      Once again, happy birthday my sweetheart. 💖
    </p>
  </div>

  <!-- Decorative image placeholders - replace these files in the same folder as this HTML -->
  <img src="balloons.png" class="decor left-balloons" alt="balloons (replace)">
  <img src="couple.png" class="decor right-couple" alt="couple (replace)">
  <img src="stars.png" class="decor top-stars" alt="stars (replace)">

  <!-- Confetti pieces generated with JS (small colorful rectangles) -->
  <script>
    // create confetti pieces
    const colors = ['#ff4d6d','#ffd166','#06d6a0','#4cc9f0','#c77dff','#ff9f1c'];
    for(let i=0;i<28;i++){
      const d = document.createElement('div');
      d.className = 'confetti-piece';
      d.style.left = Math.random()*100 + 'vw';
      d.style.background = colors[Math.floor(Math.random()*colors.length)];
      const delay = Math.random()*4;
      const duration = 4 + Math.random()*5;
      d.style.animationDelay = delay + 's';
      d.style.animationDuration = duration + 's';
      d.style.transform = 'rotate(' + (Math.random()*360) + 'deg)';
      d.style.width = (8 + Math.random()*14) + 'px';
      d.style.height = (6 + Math.random()*12) + 'px';
      document.body.appendChild(d);
    }
  </script>

  <!-- Background music: file included in the zip -->
  <audio controls autoplay loop>
    <source src="chipi_chipi_chapa_chpa.mp3" type="audio/mpeg">
    Your browser does not support the audio element.
  </audio>

</body>

  
</html>
